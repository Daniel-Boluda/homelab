apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/instance: spotTube
    app.kubernetes.io/name: spotTube
    backup: spotTube-config-zfs
    backup/retain: quaterly
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
  name: config-spotTube
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/instance: spotTube
    app.kubernetes.io/name: spotTube
    backup: spotTube-music-zfs
    backup/retain: quaterly
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
  name: music-spotTube
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi