apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: spotdl-secrets
  namespace: spotdl
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  data:
    - secretKey: ADMIN_TOKEN
      remoteRef:
        key: /spotdl
        property: ADMIN_TOKEN
