apiVersion: v1
kind: ConfigMap
metadata:
  name: server-mcp-script
  namespace: mcpservers
data:
  server_script.py: |-
{{ .Files.Get "src/server.py" | indent 4 }}
  server_mcp.json: |-
{{ .Files.Get "src/server_mcp.json" | indent 4 }}
